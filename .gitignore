# ── macOS 系统残料 ──
.DS_Store

# ── Python 编译缓存 ──
__pycache__/
*.pyc

# ── 备份文件（时间戳备份不入库）──
*.bak
*.bak.*
*.bak-*

# ── 安装包内时间戳回灌备份目录（不入库）──
记忆琥珀回灌前备份_*/

# ── clock-loop 运行时态（评估时生成，非发布产物）──
.phaseX_last_run.json
