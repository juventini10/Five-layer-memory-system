---
name: CHANGELOG
description: 三明智 历史版本变更记录
---

### v3.9.3 — 2026-07-18
+ L1 签名格式统一：三路径签名模板均加入 `| 日期`，与 L2/L3 对齐（此前 `references/shared/signature.md` 已含日期示例，仅 SKILL.md 正文 L1 表格漏写，本次补齐）

### v3.9.2 — 2026-07-13
+ compatibility `trae-solo`→`trae-work`（与其他核心Skill对齐；trae-solo→trae-work 修复模式在④⑤⑥核心Loop后成型，triwich早期②Loop时未纳入，此次补齐；裸`trae`保留为有效平台标识）。发现于 awaken-memory-system v7.14 Loop 核对 triwich 版本时的顺带发现

### v3.9.1 — 2026-07-12
+ Clock-loop 闭环修复：Poka-Yoke 步序法补充（防跳步机制新增步序法强依赖——Step0→0.5→1→2→G1→3→4串行，上步未完成不可执行下步）
+ G3 跳号正文注释（SKILL.md 第112行补注：G3已被Agent B Step3替代，编号保留不重用）
+ L3 步数格式统一（触发词表 "11步+2检查点" → "11步+1子目标+2检查点"，与L3段头+full-flow.md一致）
+ T5攻击面5维度评估落地（v5.15 §二附B）：新增评估表（D1高/D2高/D3高/D4中/D5高→攻击面高→T5要求11项全填）

### v3.9.0 — 2026-07-12
+ 正文开头格式改造（v5.15 §1.6）：删除作者署名行+创建日期描述

### v3.8.9 — 2026-07-11
+`references/l3/full-flow.md` 阶段2·全量审查新增「前提核验」子弹：凡结论依赖可测量 artifact 事实（精确行数/标记密度/阈值跨越/真实计分），✅DO 先实测或要求提供真实数字；未测则在结论标 ⚠️前提未实测，不与已验证结论同权。来源：clock-loop 缺口3 确定性脚本+沙盒实测推翻 L3 错前提（2026-07-11）。同步固化至 clock-loop/references/triwich-integration.md v3.8.9（补 L3 前提核验指针）。

### v3.8.8 — 2026-07-10
+§4.3 熔断器合规改造：2个熔断器按新规范重写（外部调用→全量五字段/内部校验→简量三字段）。配合 Skill标准规范§4.3约束式重写（2026-07-10用户确认闭环防回归）

### v3.8.7 — 2026-07-10
交叉一致性修复（clock-loop v2.6.1 评估发现问题）

- **P0修复**：License声明自相矛盾——YAML `license: Proprietary` vs 文末"开源协议：MIT"。统一为Proprietary（删文末MIT，保留YAML）
- **P1修复**（7项）：
  1. eval-set声明"16条"→实际18条（SKILL.md索引表+自检清单+eval-set头部三处同步）
  2. signature.md签名示例与SKILL.md格式不一致——L1/L2/L3示例统一为"结论块浓缩版"格式（与SKILL.md签名格式段一致）
  3. L3步数三处不一致——统一为"11步+1子目标+2独立检查点(G1/G2)"，signature.md L3示例从"10步全走+G5终检通过"改为"Agent A✅ (4/4子目标) + Agent B✅"
  4. G系列编号跳号（缺G3）——在full-flow.md头部注释说明G3=已被Agent B Step3批判验证替代（编号保留不重用）
  5. description触发词"第一性原理/拆到不能拆/主要矛盾"在触发条件段无路由规则——在触发词→深度对照表补充对应路径
  6. 索引表"8要素+9标准"幽灵声明——改为"8要素+⚔️对抗性检查"（critical-verify.md无"9标准"内容）
  7. L2触发词表"7步"错误——改为"8步"（dual-routes.md明示L2统一8步，关键设计说明已明示触发词组合不改变执行路径）
- **P2修复**（3项）：
  - L1概要"4-5步"→"4-6步·视路径而定"（批判验证实际6步）
  - CHANGELOG v3.8.6条目删过时"9.9.9"注释（frontmatter已改为3.8.6）
  - dual-routes.md L2签名格式从"双思维摘要"→"结论块浓缩版"（与SKILL.md统一）
- **依据**：clock-loop v2.6.1评估+独立审查Agent报告（发现1P0+7P1+5P2问题）
- **评估修正**：自评85.2%→子Agent审查修正为68.0%→本修复后预期回升至85%+
- **版本**：triwich v3.8.6→v3.8.7

# 变更日志

> 本文件记录当前版本之外的历史变更。当前版本信息在 SKILL.md 头部。

### v3.8.6 — 2026-07-09
L3 结论块防跳过（设计端消跳转·对齐 19:12 漏渲染事故）

- **根因**：L3 段原写"结论块和门禁锁模板同L1（见上文）"——一句软跳转，长输出里被压成散文 bullet 漏渲染（19:12 实跑已发生），属 §一1.1「跳转引用=设计端漏洞」。
- **修复（设计端防误·消跳转）**：
  1. SKILL.md L3 段：删软跳转，内联结论块卡槽（`## 🔒 L3 结论块` 段头 + `| 结论 | 说明 |` 表 + 关键发现/下一步），明示"不得压成散文 bullet"。
  2. full-flow.md 结论块段：删"→ 见SKILL.md"软跳转，改内联 3 条硬约束（独立段头 / 强制表格 / L3 额外引用 G1+Agent B），门禁锁3问显式挂签名前。
- **边界**：①② 属设计端降漏，仍非确定性 100%（LLM 概率性）。
- **③ 确定性拦截层（2026-07-09 同日闭环·WB平台局限已探明）**：发现 WorkBuddy 原生 `Stop` 钩子（settings.json 注册 + 退出码 2 物理拦截），绕开 hookify 插件依赖。已交付：`~/.codebuddy/hooks/l3_conclusion_guard.py`（Python 守卫脚本）+ 两侧 `settings.json`（`~/.codebuddy/` + `~/.workbuddy/`）已注册。5 用例离线测试全过。**已探明平台边界**：WorkBuddy 5.2.5 不支持用户级 Stop 命令钩子（`~/.workbuddy/settings.json` hooks 不执行·trace 零条执行日志·两次重启实测确认）。CodeBuddy Code 侧 `/hooks` 已识别该钩子。脚本+注册留位，WB 未来版本支持后重启即活。③层当前不可行结论经用户确认。
- **版本**：triwich v3.8.5→v3.8.6

### v3.8.5 — 2026-07-09
L3 内联转发门禁锁（根治「主agent只给结论块+present_files、推导不进对话」·对齐 MEMORY 第74行用户偏好）

- **根因**（第一性原理推导）：v3.8.4 把主agent 收为纯 orchestrator（启动+转发+签名），但「转发 Agent A 推导进对话」仅为角色描述软约束，无签名前置校验锁。07-09 实跑 L3 评估改造方案时，主agent 把推导写 /tmp 文件 + present_files，对话只给结论块+Step4+签名 → 违反 MEMORY 第74行「L3 推导内联不写文件」用户偏好。根因非「段头写反」（v3.8.4 已锁），而是「主agent 不内联转发推导」——该动作无门禁锁拦截。
- **关键认知修正**：上轮拟「删文件通道旧指令」不可行——Agent B 是独立子Agent（看不到主聊，见 full-flow 因果链），其输入强制从 /tmp 文件读（阶段1盲审+阶段2全量），文件通道对 Agent B 是**架构必需**不可删。真治本非删文件，而是把「主agent 内联转发推导」升级为硬门禁锁 + 明确文件仅服务盲审。
- **修复（设计端防误·角色缺失法 + 接触式门禁锁）**：
  1. full-flow.md 主agent角色锁接口② 从「转发 Agent A 的内联产出」强化为「**把 Agent A 完整推导（段头+8要素）原样内联进对话**；⛔DO NOT 以 present_files/文件链接替代」。
  2. full-flow.md 正面建模段 + Agent A 产出写入段 明示「文件通道仅因 Agent B 子Agent 架构需要而保留为盲审输入源，绝不替代对话内联；误把推导只存文件+present_files 而对话不内联 = 违规」。
  3. SKILL.md L3呈现骨架锁段 新增「🔴 L3内联转发门禁锁」：主agent 签名前必须已内联转发 Agent A 完整推导（段头+8要素），未内联即输出签名 = 违规（类比结论块门禁锁）。
- **依据**：Skill架构规范 §一1.1 Poka-Yoke（防误>防错>事后纠错）+ 用户 MEMORY 第74行「L3 内联不写文件」偏好（2026-07-08 定）；指令编写规范 v4.1 §二 因果链。
- **边界**：门禁锁属防错层，LLM 概率性可能漏（规范§一1.1 明示）；平台级「subagent 直出对话」需改 Agent 工具，超出 Skill 范围。文件通道保留为 Agent B 盲审必需，不删。
- **版本**：triwich v3.8.4→v3.8.5

### v3.8.4 — 2026-07-08
L3 收口主agent为纯orchestrator（根治「主agent链外自由写反序」·第一性原理最优解）

- **根因**（第一性原理推导）：呈现源仅两类——Agent A（子agent·执行链内·可接触法锁）/ 主agent（LLM·自由文本·不可物理锁）。08:40 事故 = 主agent 在 Skill 执行链之外自由写反序段头，因其拥有「写步骤」的隐含权限。主agent 是 LLM，自由文本能力物理不可剥夺 → 主agent 侧任何约束只能是防错法，到不了接触法。
- **最优解**：v3.8.3 已在 Agent A 侧用接触法锁死段头（物理不可造=治本）；主agent 侧当下能到达的最强约束 = **角色缺失法**（比行为禁令#7更强）：在架构/规范层删除主agent 的「写L3步骤」角色，主agent 只留 启动+转发Agent A内联+签名 三个接口。步骤叙述在 Skill 定义中无主agent 位置 → 无接口可造反序。
- **修复（设计端防误·角色缺失法 优先于 行为禁令）**：
  1. full-flow.md「预执行：启动声明」段新增「🔴 主agent角色锁」——主agent 只拥有三接口（启动声明/转发Agent A内联/签名），无步骤叙述权限；⛔DO NOT 由主agent 在链外概述/复述 L3 流程（无论是否带 Step 编号）。
  2. SKILL.md L3呈现骨架锁段补「段头由 Agent A 在执行链内写出；主agent 无 Step 段头编写接口，只转发不另写」。
  3. SKILL.md 禁止行为#7 升级——从「禁带Step编号复述」扩到「禁主agent 在 L3 写任何步骤叙述（含不带编号的反序自由文本）」。
  4. eval-set.md 新增 #18 用例专验主agent角色锁。
- **依据**：Skill架构规范 v5.9 §🎯 防误>防错（角色缺失法>行为禁令>事后纠错）+ §1.1 Poka-Yoke + §6.4 eval mandatory；指令编写规范 v4.1 §二 因果链 + §5.8 Alternatives Considered + §6.6 备份；三大思维白皮书 v3.9 §1.4/§3.3/§3.6。
- **Alternatives Considered**：
  | 设计决策 | 选择 | 替代方案 | 为什么不选 |
  |:---------|:-----|:---------|:----------|
  | 主agent侧防反贴 | 角色缺失法（删步骤叙述权限接口） | 仅加更多行为禁令 | 禁令仍可绕（不带编号的反序自由文本），08:40已证可违反 |
  | | | 改名「第一性原理」→「第二性原理」 | 破坏白皮书术语一致性，治标 |
  | | | 让主agent自由文本也接触法锁 | 物理不可能（LLM本质） |
- **eval 核验**：eval-set #1-17 全 PASS 无回归（触发词/运行时机制未动）；#18 专验主agent角色锁拦截。备份：SKILL.md + full-flow.md + eval-set.md 改动前快照存 `[记忆共享中心]/记忆琥珀/技能升级备份/`（合规§6.6）。
- **合规核验**：对照 Skill架构规范 v5.9 + 指令编写规范 v4.1 全通过；版本单一来源 = YAML v3.8.4 = CHANGELOG 最新条目一致（自检清单#1）。
- **边界**：主agent 在 L3 之外的口头对话仍可能说错（LLM本质，不可锁，超出 Skill 呈现层，可接受）。⚠️ 已知未知：WorkBuddy 加载/压缩机制是否真让主agent 只发≤2行并遵守角色，需实跑验证。
- **版本**：triwich v3.8.3→v3.8.4

### v3.8.3 — 2026-07-08
L3呈现骨架锁 + 禁止行为#7（根治「Step1/Step2段头反贴」设计漏洞）

- **根因**（2026-07-08 实测）：主agent在 L3 评估中将 Step1=第一性原理、Step2=抽象建模 写反。根因有二——
  ① 命名陷阱：「第一性原理」含"第一"但执行于 Step2，反直觉且无对冲；
  ② 主agent在 Skill 执行链之外自由重述带 Step 编号的流程，逃逸出产出物锁/结论块锁/完整性锁的 Poka-Yoke 边界，编号可任意贴导致顺序写反。
  用户已对照双重权威源（三大思维白皮书 v3.9 §1.4/§3.3 + 口诀"先定向→再分类→再拆解"）确认正确顺序为 Step1=抽象建模 → Step2=第一性原理。
- **修复（设计端防误·接触法 优先于 事后纠错）**：
  1. SKILL.md L3 段头新增「🔴 L3呈现骨架锁」——✅DO 复用白皮书§3.6 固定段头骨架（【抽象建模】=Step1 / 【第一性原理】=Step2 / 【苏格拉底诘问】=G1 / 【批判验证】=Step3 / 【具象落地】=Step4），段头写死不可改，主agent只填内容。
  2. SKILL.md 禁止行为表新增 #7——⛔DO NOT 在 Skill 执行链之外自造带 Step 编号的 L3 流程复述（反贴即错）。
  3. full-flow.md「🔴对话内联呈现」段补「Step 段头写死」——物理绑定段头与思维模块，反贴即错。
- **依据**：Skill架构规范 v5.9 §🎯 防误>防错（接触法优先）+ §1.1 Poka-Yoke + §6.4 eval mandatory；指令编写规范 v4.1 §5.7 Poka-Yoke + §二 因果链（每条约束带"原因是"）+ §5.8 Alternatives Considered + §6.6 备份；三大思维白皮书 v3.9 §1.4/§3.3/§3.6。
- **Alternatives Considered**：
  | 设计决策 | 选择 | 替代方案 | 为什么不选 |
  |:---------|:-----|:---------|:----------|
  | 防顺序写反 | 设计端骨架锁（接触法）+ 检查端禁令#7 双保险 | 仅加「自检锁」让主agent执行后自查顺序 | 自检锁属事后纠错，主agent仍可先写反再"自查"，错误已产生；接触法让写反在物理上不可能 |
  | | | 仅收口权（要求必须引用白皮书段落） | 引用≠不写反，段头仍可自造；无硬绑定 |
  | | | 改名「第一性原理」→「第二性原理」消除命名陷阱 | 破坏白皮书术语一致性，牵一发动全身，成本高于锁 |
- **eval 核验**：eval-set 16条全 PASS 无回归（触发词/运行时机制未动）；新增 #17 用例专验骨架锁拦截反贴。备份：SKILL.md + full-flow.md + eval-set.md 改动前快照存 `[记忆共享中心]/记忆琥珀/技能升级备份/`（合规§6.6）。
- **合规核验**：对照 Skill架构规范 v5.9（原则1-4/反模式D1-D5/§6.4）+ 指令编写规范 v4.1（五级标注/因果链/Alternatives/§6.6 备份）全通过；版本单一来源 = YAML v3.8.3 = CHANGELOG 最新条目一致（自检清单#1）。
- **版本**：triwich v3.8.2→v3.8.3

### v3.8.1 — 2026-07-08
L3对话内联呈现硬约束（根治呈现弱于L1/L2）
- 新增：🔴对话内联呈现硬约束段——L3全流程思考（Step0判定/锚点两列对立面/苏格拉底五问/8要素表/Agent B盲审8要素表+全量锚点推导验证+交叉终判/结论块）须内联显示于对话，呈现完整度必须≥L1/L2
- 修正：Agent A产出从"写入文件系统"改为"内联对话（文件可选通道）"；文件定位降级为Agent B可选传递通道，明确"内联不破坏盲审"
- 依据：指令编写规范（五级标注+因果链+正面建模）+ Skill架构规范（Alternatives Considered段）
- 版本：triwich v3.8.0→v3.8.1

### v3.8.1 补 —— 历史债务修复（索引表不一致 + eval-set执行）
- 修复索引表（SKILL.md §references/目录索引）：`shared/8-elements.md`→`shared/8element.md`（拼写对齐实际文件）；删除不存在的 `shared/gate-checks.md`（门禁锁/熔断器规则已内联于§防跳步机制）；补列 `shared/anchor.md` 与 `reviewer-protocol.md`（实际存在但漏列）；自检清单#4 同步改 `8-elements`→`8element`
- 执行 eval-set 16条用例：1-10 按触发词表+语境限定表静态执行全 PASS；11-16 按§防跳步机制/状态表/禁止行为/reviewer-protocol 机制追溯全 PASS；其中 L1/L3 运行时路径以实跑验证（见执行报告）
- 备份：full-flow.md 改动前快照已存 `[记忆共享中心]/记忆琥珀/技能升级备份/triwich_full-flow_v3.8.1_20260708.bak`（合规§6.6）

### v3.8.2 — 2026-07-08
L3-Agent B 对抗性验证补丁（根治 v3.7.0 改造漏落地）
- 修复：reviewer-protocol.md 阶段2 新增「2d. 对抗性验证」——按 `critical-verify.md` Step3 执行（委派单一来源，不复制四步原文）。根因：v3.7.0 仅更新白皮书 L3 模板描述，未打补丁到执行协议，致 L3 实际零对抗
- full-flow.md Agent B Step3 段点名"含⚔️对抗性验证（委派 critical-verify Step3）"，并阶段2列表补对抗行，焊死白皮书意图与执行文件一致性
- 更正：上轮"reviewer-protocol 8要素与主8要素错配"为误判——实读 shared/8element.md 确认其8要素与 L1/L2 共用模板完全一致，无错配，故该步不执行（撤销）
- 三层一致性：L1 原生 / L2 委派 / L3-Agent B 委派，均指向 critical-verify.md Step3 单一来源，杜绝再漂移
- 备份：reviewer-protocol.md + full-flow.md 改动前快照存 `[记忆共享中心]/记忆琥珀/技能升级备份/`（合规§6.6）
- 依据：指令编写规范（五级标注+因果链+正面建模+单一来源）+ Skill架构规范（Alternatives/防漂移）
- 版本：triwich v3.8.1→v3.8.2
- 合规核验：full-flow.md「🔴对话内联呈现」段对照指令编写规范 v4.1（五级标注/因果链/正面建模/Alternatives）+ Skill架构规范 v5.9（原则1-4/反模式D1-D5/Alternatives段）全通过

### v3.8.0 — 2026-07-05
隐含假设扫描（四象限框架落地）
- 新增：所有层级Step0.5隐含假设扫描——AI推断3条用户隐含假设，追加写入step0-determination.md
- 防御机制：模板锁（4字段×3条）+ 熔断器（标准格式含降级方案）+ 自爆（全员🟢→强制重推）
- L3增强：子目标拆解的子问题③直接引用Step0.5的3条假设，自爆升级
- 触发表同步更新：所有层级最少步骤+1
- 版本：triwich v3.7.0→v3.8.0

### v3.7.0 — 2026-07-05
批判验证新增对抗性检查（白皮书v3.8对抗性验证落地）
- 新增：L1批判验证步骤3：⚔️对抗性检查（假设推演→反方论证→边界条件→综合判断）
- 白皮书同步：§1.3新增对抗性验证溯源（Popper+Pre-mortem+Steelmanning），§3.3 Step3更新，L3输出模板更新
- 版本：白皮书v3.7→v3.8，triwich v3.6.1→v3.7.0

### v3.6.1 — 2026-07-03
clock-loop评估驱动的合规补全
- 新增：上下文级宪法声明（依据指令编写规范 §5.5 v3.7+）
- 新增：references/ 目录索引表（10个文件+加载时机）
- 新增：eval-set.md 评估集（16条用例：触发/不触发/运行时行为）
- 新增：自检清单（8项，修改Skill后必过）
- 修正：正文元数据行删除版本号变更描述（单一来源原则——YAML version 是唯一权威源）
- 修正：防跳步机制补五级标注（☑SHOULD/⛔DO NOT）
- 修正：来源与参考表补可靠性分级（🟢🟡）
- 修正：「与其他Skill关系」段开发规范引用去掉版本号后缀

### v3.6.0 — 2026-07-02
Step0→Step1熔断器升级——格式排他性+物理阻断
- 原上步校验锁（声明"✅DO Read文件确认"）→ Step0文件熔断器（物理阻断）
- 读不到Step0文件 → 中止全过程，不自欺欺人补写
- 格式混入其他Skill标记（如【意图分类】）→ 退回重写
- 新增：⛔DO NOT混入【意图分类】等格式——Step0判定只使用"三明智 {L1/L2/L3} 路径：{路径名}已启动"格式
- 跨Skill格式污染预防（来源：Step0步行故障根因修复）

### v3.3.1
Step0→Step1衔接段新增上步校验锁（三级级联锁模式反哺自每日伙伴）——确认step0-determination.md非空+深度标注与实际路由一致

### v3.3
按Skill架构规范v3.1+指令编写规范v2.1全量合规改造

### v3.2
全层级路径声明审计修复——L1/L2/L3 all加"对话第一条输出"的路径声明强制字段

### v3.0
双Agent架构——Agent A（执行者）负责Step0→G1，Agent B（验证者）独立执行Step3。盲审设计+文件系统传递+Agent B独立WebSearch。
