# 每日伙伴·步骤6"次日骨架不可跳过"硬防护改造（方案B·已落地）

> 状态：已落地（2026-07-18，用户批"go"）
> 提出：2026-07-18，用户第N次指出"补做复盘跳过步骤6/不生成次日骨架"
> 设计依据：Skill架构规范 §1.1 防误>防错 + §2.1 轻量化优先 + 指令编写规范 §5.5 路由级降级防御 + §5.7 防误优先 + §6.1 单一来源

---

## 一、根因诊断（不是Skill缺陷，是防护层级被绕过）

| 层 | 现状 | 问题 |
|----|------|------|
| 防误（设计端·应优先） | 步骤7 L131-134 已有物理校验锁：进步骤7前须Read步骤6骨架，不存在→🔴物理阻断·不得以任何理由降级 | 锁设计正确，但被"整体跳过工作流C"绕过——AI把补做复盘当非正式任务，锁因未进入流程而从未触发 |
| 规范层级 | 步骤6 原是路由级（"不可拆分"但AI有判断空间） | 给了AI自创"日期已过豁免"的口子 |

**核心错配**：失效不在锁强度，而在AI根本没跑流程。治本=剥夺"自创豁免"的判断空间（路由级升系统级），不是加新脚本。

**用户硬性诉求（宪法级前提）**："不管写什么时候的日记，就必须要生成次日模板+灵感闪现，没有理由违背跳过。" → 定值法表述：**对任意被复盘的日期 D，文件 D+1 的骨架必须存在且含灵感闪现**。不区分"当日/追溯"。

---

## 二、方案选型：A vs B（以顶级Skill设计大师视角）

| 维度 | A：新建 finalize_review.py 收盘闸门(~+95行) | B：折进现有步骤6/7校验锁(~+22行) |
|------|------|------|
| 对准根因 | 否——只加强锁强度，AI连流程都没跑则脚本不会被调 | 是——消除自创豁免的判断空间 |
| 轻量化（§2.1） | 否——新增独立脚本 | 是——复用 create_skeleton，零新脚本 |
| 行数经济 | 否 | 是 |
| 规范层级升级（§5.5） | 含 | 含（更直接） |

**选定 B**：A治标不治本，B治本且契合§2.1轻量化优先+§5.5降级防御+§5.7防误优先。

---

## 三、实际落地的四处改动（2026-07-18）

### 改动1：工作流C-晚间复盘.md 步骤6 头部 + 6.1
- 步骤6标题下新增系统级硬约束块：⛔DO NOT 跳过（无论复盘哪天/追溯）+ 路由级升系统级声明。
- 6.1 增加幂等guard：次日文件已存在→⛔DO NOT覆盖，仅Read校验「💡灵感闪现」就位且非占位符；缺失按步骤2兜底补入。不存在→调 create_skeleton 写。

### 改动2：工作流C-晚间复盘.md 步骤7 校验锁（L131-134）
- 路径占位由 `YYYY-MM-DD.md` 改为 `次日日期.md`。
- 锁末补：⛔DO NOT 以"复盘的是过去某天"为由跳过本锁——对所有日期D一视同仁，D+1骨架必须存在。

### 改动3：SKILL.md L372 完整性锁
- "次日骨架Read回验通过" → "次日骨架已建且Read回验通过（无论复盘哪天/追溯均适用·⛔DO NOT以日期已过豁免）"。

### 改动4：本方案文档由A版同步为B版（单一来源，不残留A）。

---

## 四、幂等与边界

- 同一天多次复盘：次日md存在则校验不重建，不覆盖。
- 7/17 这类已建好的：识别存在→只校验灵感闪现就位→通过给凭证，不破坏已有内容。
- 跨月：D=07-31→N=08-01，create_skeleton 按 YYYY-MM 子目录自动落正确月目录。
- **诚实边界**：B仍依赖AI实际调用工作流C；T5不可绕过声明(L78)已背书"无例外"，B是把该背书在步骤层显式化。

---

## 五、验证

- 对 7/16 复盘重跑步骤7锁：7/17 已存在→校验灵感闪现就位→给凭证通过，确认幂等、不覆盖。
