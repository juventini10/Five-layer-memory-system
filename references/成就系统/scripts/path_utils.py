#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
统一路径配置模块 —— 单点修改，全脚本生效

用途：所有成就脚本从此导入日记/成就系统路径，而非各自硬编码。
日记子路径因人而异，统一用 `[日记子目录]` 占位符，安装 step6 自动替换，不用改4个脚本。

安装时 step6 的 sed 命令自动替换 [日记仓库] / [记忆共享中心] 占位符为实际路径。
"""
from pathlib import Path

# ═══════════════════════════════════════════════════════════════
# 路径配置 —— 用户安装时只需修改这里
# ───────────────────────────────────────────────────────────────
# DIARY_DIR = Path("[日记仓库]/[日记子目录]")
#   [日记仓库]   = 你的日记仓库根目录（安装 step6 自动替换）
#   [日记子目录] = 仓库根到日记目录的相对路径（如 1-每日计划/01-日记）
#                  子路径因人而异，故做成占位符，安装 step6 提示填入
# ═══════════════════════════════════════════════════════════════
DIARY_DIR = Path("[日记仓库]/[日记子目录]")
ACHIEVEMENT_DIR = Path("[记忆共享中心]/成就系统")
