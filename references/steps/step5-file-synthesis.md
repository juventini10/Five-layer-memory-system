# 步骤5：个性化文件合成

📦 **产出物**：所有个性化文件已创建（摘要版认知层段/行为层段/协作契约段 + SOUL.md品格注入 + USER.md + CORE.md + SHADOW.md + MEMORY.md通用骨架 + 动态状态快照）

---

> **⚡ 步骤锁定协议**
> ① 导航锁：每次回复首行输出 `步骤5/7`
> ② 偏离锁：用户问无关问题 → 记到待办列表，继续合成
> ③ 检查点前置依赖：Read `{记忆共享中心}/.install/step4-done.md` — 不存在则阻塞
> ④ 检查点写入：末尾 Write `{记忆共享中心}/.install/step5-done.md`

## 执行指令

### ⚠️ 禁止行为

| 禁止 | 原因 |
|:----|:-----|
| ❌ NEVER未经用户确认就写入文件 | 答题数据→文件的映射可能偏差，偏差会向下污染所有Skill |
| ❌ NEVER混用写作风格 | SOUL用MUST/NEVER=品格污染，摘要版用身份宣言=语义不对 |
| ❌ NEVER跳过风格合规检查（5f） | 风格错误=文件语义错误=AI行为不对，写入后再改成本更高 |

### 🔌 熔断器

- 成功条件：所有目标文件已写入 + 风格合规检查通过 + 用户确认完成
- 中断处理：任一步骤出错 → 输出已完成文件清单 + 标注失败点 → 引导回到对应子步骤修复
- 重试机制：修正后从失败的子步骤继续，不需要重跑整个step5
- 降级处理：模板文件缺失 → 输出缺失清单 → 从其他可用模板补全骨架 → 标注"降级合成"

### 5a. 数据来源切换

> ⚠️ 本步骤所需33题数据从 `{记忆共享中心}/.install/问卷答案汇总.md` 读取（由步骤4写入并经过两段验证），不再依赖AI上下文。文件缺失 → 回退步骤4补答。

1. Read `{记忆共享中心}/.install/问卷答案汇总.md`，提取Q1-Q33答案
2. Read `references/templates/` 下的对应模板文件

### 5b. 用户类型分支

```
├─ 全新安装 → 走5b文件合成（答题数据 + 干净通用模板 → 个性化文件）
└─ 升级     → 走5g升级合并（备份旧系统文件 → 模板骨架+旧数据注入 → 写入新版）
```

---

### 5b. 答案写入规则

> **产出来源说明**：Step2预问卷标记为`(step2)`，33题问卷标记为`(Q1-Q33)`。

#### 5b-1. Step2预问卷 → 用户基础信息

| 来源 | 信息类型 | 目标文件 | 目标位置 |
|:---:|:--------|:--------|:--------|
| Step2 Q1 | 称呼 | USER.md | 头部"称呼"字段 |
| Step2 Q2 | 一句话身份简介 | USER.md | "关于你"段 |
| Step2 Q3 | 最希望AI解决的问题 | USER.md | "当前关注"段 |
| Step2 Q4 | 署名表情 | IDENTITY.md | Emoji字段 + 署名格式行 |

#### 5b-2. 33题v4.0 → 个性化文件注入

| 题号 | 题目内容 | 信息类型 | 目标文件 | 目标位置 |
|:---:|:--------|:--------|:--------|:--------|
| Q1 | 善意的谎言（勉强接受机会时对自己说了什么） | 自我欺骗模式 | CORE.md | L1核心层段 |
| Q2 | 佩服的朋友/熟人特质 | 价值观锚点 | CORE.md | L1核心层段 |
| Q3 | 为重要目标放弃过什么 | 取舍观 | CORE.md | L1核心层段 |
| Q4 | 用颜色+天气形容自己和精力 | 情绪基调+精力模式 | CORE.md | L1核心层段 |
| Q5 | 投入最多的事突然做不了的第一反应 | 抗挫模式/路径重构 | CORE.md | L1核心层段 |
| Q6 | 最大认知盲区 | 自我认知 | CORE.md | L1核心层段 |
| Q7 | 3年后的最大区别 | 未来期许/动力来源 | CORE.md | L1核心层段 |
| Q8 | 搞砸/失败时归咎于什么 | 归因模式 | 摘要版 | 认知层表一行 |
| Q9 | 上一次"改变主意"是因为什么 | 认知升级触发 | 摘要版 | 认知层表一行 |
| Q10 | 什么情况下会硬撑 | 坚持条件 | 摘要版 | 认知层表一行 |
| Q11 | 想清楚再做还是做了才知道 | 决策风格 | 摘要版 | 认知层表一行 |
| Q12 | 怎么判断建议值不值得听 | 判断标准 | 摘要版 | 认知层表一行 |
| Q13 | AI方案和想法不一样怎么处理 | AI协作分歧 | 摘要版 | 认知层表一行 |
| Q14 | 犯错/内耗时希望AI怎么对你 | 错误应答偏好 | SOUL.md | 「我怎么说话」段 |
| Q15 | 最喜欢AI反馈信息的方式 | 反馈格式偏好 | 摘要版 | 认知层表一行 |
| Q16 | 让AI整理资料时偏好的呈现方式 | 表格控排版偏好 | SOUL.md | 「我怎么说话」段 |
| Q17 | AI发现逻辑漏洞时希望怎么做 | AI边界感 | 摘要版 | 认知层表一行 |
| Q18 | 社会时差类型 | 生物钟/作息 | 摘要版 | 行为层表一行 |
| Q19 | 工作/创作时喜欢的节奏 | 工作节奏 | 摘要版 | 行为层表一行 |
| Q20 | 最容易中断工作的事 | 注意力杀手 | 摘要版 | 行为层表一行 |
| Q21 | 怎么处理待办事项 | 待办管理 | 摘要版 | 行为层表一行 |
| Q22 | 什么情况下主动学习新东西 | 学习触发 | 摘要版 | 行为层表一行 |
| Q23 | 最近一周最有成就感的事 | 成就感来源 | 摘要版 | 行为层表一行 |
| Q24 | 接下来3个月必须达成的核心目标 | 北极星目标 | USER.md + 动态状态快照.md | 「当前状态」段 + L4快照 |
| Q25 | 与核心目标相关但一直拖着没做的事 | 拖延事项 | 动态状态快照.md | L4拖延段 |
| Q26 | 达成目标的最大瓶颈 | 当前卡点 | 动态状态快照.md | L4瓶颈段 |
| Q27 | 今天/本周最迫切推进的具体任务 | 紧急任务 | 动态状态快照.md | L4任务段 |
| Q28 | 现在最需要什么 | 当前需求 | USER.md | 「当前需」段 |
| Q29 | 哪些场景容易做出违背计划的事 | 翻车场景 | SHADOW.md | L5偏差初始记录 |
| Q30 | 言行不一致时希望AI怎么处理 | 对质偏好 | SHADOW.md | L5对质规则 |
| Q31 | 如果让AI永远记住的一句话 | 金句铁律 | 摘要版 | 协作契约表一行 |
| Q32 | 题目不舒服/希望AI永远知道的 | 补充诉求 | 摘要版 | 协作契约表一行 |
| Q33 | 过去一个月违背自己原则的行为 | 原则违背 | 摘要版 | 协作契约表一行

---

### 5c. 写作风格映射（关键！严格按此执行）

> ⚠️ 不同文件使用不同写作风格。走错风格 = 品格污染 = 规则语义不对。NEVER混用。

**风格规范文件引用**（只写文件名，不写版本号）：
- 品格式 → `品格设计通用规范`（身份宣言+行为印证，NOT MUST/NEVER）
- 规则式 → `指令编写规范`（双轨制+因果链+数值锚定）

| 目标文件 | 写作风格 | 依据规范 | 核心原则 | 禁止 |
|:--------|:-------|:--------|:--------|:----|
| SOUL.md | **品格式** | `品格设计通用规范` | 身份宣言（"你是一个会XX的人"）+ 行为印证（"第一条回复永远是意图分类"） | MUST/NEVER/禁止/CRITICAL |
| 摘要版 | **规则式** | `指令编写规范` | 简短执行指令，AI可直接执行。"帮/必须/NEVER + 原因" | 废话/背景说明/个人叙事 |
| CORE.md | **声明式** | — | 一句价值观一句话，身份+底线 | 冗长解释 |
| USER.md | **描述式** | — | 纯用户画像，"用户是什么样的人" | 写指令/写规则 |
| SHADOW.md | **记录式** | — | 客观记录偏差，无价值判断 | 评价/建议 |

**品格式 vs 规则式 对比示例**：

```
正确（品格式 → SOUL.md）：
  "你是一个会先安抚情绪再给建议的人——因为用户在犯错时需要情绪支持，不是冷分析。"

正确（规则式 → 摘要版）：
  "犯错时：NEVER直接跳到冷分析——原因是用户需要先处理情绪。先安抚，再给建议。"

错误（规则式写进SOUL.md）：
  "犯错时：NEVER直接跳到冷分析" ← 这是在SOUL里写MUST，品格污染

错误（品格式写进摘要版）：
  "你是一个会先用表格呈现信息的人" ← 这是在规则里写身份宣言，语义不对
```

---

### 5d. 文件合成流程

按以下顺序逐个文件生成：

#### Step 5d-0：IDENTITY.md 生成

读取 `references/templates/IDENTITY.md` 通用骨架 → 填入Step2预问卷数据：
- 工具名：WorkBuddy
- Name: 助理
- 核心身份：用户的AI搭档
- Emoji：{Step2 Q4答案，默认⚡}
- 署名格式：`—— 你的 WorkBuddy {Emoji}`

📍 **写入目标**：`~/.workbuddy/IDENTITY.md`（平台系统文件专属落点，**非** `[记忆共享中心]`）

> 声明式写法：一句身份一句数据，不写MUST/NEVER。

#### Step 5d-1：摘要版合成

从 `templates/用户基本规则-摘要版.md` 读取干净的通用模板 → 将答题答案按答案写入规则注入认知层表/行为层表/协作契约表。

**认知层表格式**（每个Q8-Q17一行）：
```
| Q8 | 失败归因 | [用户答案简写] | [提炼一条执行规则] |
```

**行为层表格式**（每个Q18-Q23一行）：
```
| Q18 | 社会时差 | [用户答案简写] | [提炼一条执行规则] |
```

**协作契约表格式**（每个Q31-Q33一行）：
```
| Q31 | 金句 | [用户答案原文] | [提炼一条执行规则] |
```

> 规则式写法：简短执行指令，用NEVER/必须/帮/先XX等，附原因（因果链）。

#### Step 5d-2：SOUL.md 品格注入

读取 `references/templates/SOUL.md` 通用骨架 → 填入答题数据至 `{...}` 占位符：
- 核心价值观 ← Q1-Q7（替换`{核心价值观——由Q1-Q7答题生成}`）
- Q14 → 「我怎么说话」段：以品格式写入错误应答偏好
- Q16 → 「我怎么说话」段：以品格式写入排版偏好
- Q30 → 「我是谁」段：以品格式写入对质规则（替换`{从Q30答题生成的个性化对质规则}`）
- 反馈偏好 → 「我怎么说话」段（替换`{从Q14/Q16答题生成的反馈偏好规则}`）

📍 **写入目标**：`~/.workbuddy/SOUL.md`（平台系统文件专属落点，**非** `[记忆共享中心]`）

> 品格式写法：身份宣言（"你是一个会XX的人"）+ 行为印证（自然描述该行为的结果）。
> NEVER使用MUST/NEVER/禁止/CRITICAL——品格不是规则，品格是你是谁。

#### Step 5d-3：USER.md 生成

读取 `references/templates/USER.md` 通用骨架 → 填入答题数据至 `{...}` 占位符：
- 称呼 ← Step2 Q1
- 一句话身份简介 ← Step2 Q2
- 当前关注（核心目标） ← Q24
- 当前需求 ← Q28
- 认知风格段 ← 从Q8归因/Q9驱动/Q12判断标准/Q13争议处理提取描述

📍 **写入目标**：`~/.workbuddy/USER.md`（平台系统文件专属落点，**非** `[记忆共享中心]`）

> 描述式写法：客观描述用户特质，不写任何指令/规则/建议。

#### Step 5d-4：CORE.md 生成

从Q3-Q7答题数据生成CORE.md → 价值观+自我认知+取舍观。

> 声明式写法：一句价值观一句话，不写MUST/NEVER。

#### Step 5d-5：SHADOW.md 初始化

从Q29-Q30答题数据生成SHADOW.md初始骨架 → L5偏差记录。

#### Step 5d-6：MEMORY.md 通用骨架

读取 `references/templates/MEMORY.md` → **文件写入 `~/.workbuddy/MEMORY.md`**（平台系统文件专属落点）。模板内的 `{记忆共享中心}` 占位符仍按 step3 实际**归档**路径替换（指向 `[记忆共享中心]`，**绝不**指向 `~/.workbuddy/`）。
无需答题数据——硬路牌（意图分类+产出签名+铁律骨架指针）+ 写入纪律（三问）+ 路径索引。

#### Step 5d-7：动态状态快照

从Q24-Q28答题数据生成初始动态状态快照。

#### Step 5d-8：复制通用模板（无需答题数据）

以下文件直接从 `references/templates/` 复制（不包含用户信息）：

| 目标文件 | 模板来源 |
|---------|---------|
| `用户基本规则-铁律版.md` | `references/templates/用户基本规则-铁律版.md` |
| `用户基本规则-详解存档版.md` | `references/templates/用户基本规则-详解存档版.md` |
| `记忆蓝图/`（完整目录） | 检测是否存在：不存在→`cp -r references/templates/记忆蓝图/ [记忆共享中心]/记忆蓝图/`；存在（异常）→按#23统一逻辑处理（见5g-2） |
| `最伟大的我/`（完整目录·8子目录骨架） | `cp -r references/最伟大的我/ [记忆共享中心]/最伟大的我/` |
| `情境层/动态状态快照.md` | `references/templates/情境层-动态状态快照.md` |
| `记忆规则/TTL注册表.md` | `references/templates/TTL注册表.md`（新建骨架） |
| `IDENTITY.md`（→ `~/.workbuddy/IDENTITY.md`） | `references/templates/IDENTITY.md`（合成+注入） |
| `SOUL.md`（→ `~/.workbuddy/SOUL.md`） | `references/templates/SOUL.md`（合成+注入） |
| `USER.md`（→ `~/.workbuddy/USER.md`） | `references/templates/USER.md`（合成+注入） |
| `MEMORY.md`（→ `~/.workbuddy/MEMORY.md`） | `references/templates/MEMORY.md`（合成+替换占位符） |
| `未知未知/`（完整目录·悬置区容器） | `cp -r references/未知未知/ [记忆共享中心]/未知未知/`（含悬置区.md/显形台账.md/断言台账.md；route_unknown_unknown.py 钩子写入目标） |

### 5e. 用户确认

写入前必须请用户确认所有生成的文件：

1. 展示完整的摘要版（认知层+行为层+协作契约三个表格）
2. 问："以上是根据你的问卷答案提取的核心规则。有没有跟你想的不一样的？有的话现在改。"
3. 用户认可或提出修改 → 根据反馈调整
4. ⚠️ 未经用户确认不得写入——偏差会向下污染所有Skill

### 5f. 风格合规检查（强制）

写入前执行一次风格合规检查：

| 检查项 | 检查逻辑 | 违反即修正 |
|:------|:--------|:---------|
| SOUL.md 无 MUST/NEVER | grep MUST/NEVER/禁止/CRITICAL → 有则改为品格式 | ⚠️ 有违反 → 修正后重检 |
| SOUL.md 使用"你是一个"句式 | 检查品格声明是否以身份宣言开头 | ⚠️ 缺少 → 补充品格头 |
| 摘要版 使用规则式 | 认知层/行为层/协作契约表每行以"帮/NEVER/必须/先"开头 | ⚠️ 品格式短语 → 改为规则式 |
| USER.md 无指令 | 不包含"必须/NEVER/禁止/帮"等指令词 | ⚠️ 包含 → 改为描述式 |

> ✅ 全部通过 → **分两路写入**：
> - **4 个系统文件（IDENTITY / SOUL / USER / MEMORY）→ `~/.workbuddy/`**（平台系统文件专属落点；写别处即非系统文件，不生效）
> - 其余内容文件（摘要版 / CORE / SHADOW / 记忆规则 / 情境层 / 记忆蓝图 / 最伟大的我 ）→ `[记忆共享中心]/`

---

### 5g. 升级用户：系统文件升级合并

> 🔴 **宪法级声明：本章节执行的是系统核心文件（SOUL/IDENTITY/USER/MEMORY）的升级。**
> 🔴 **不是建议，是硬约束。不可跳过。**
> 🔴 **这三份文件承载用户的价值观、品格和行为约束——比任何Skill、任何规则都重要。**
>
> **门禁铁律**：
> ① 备份 → 比对 → 执行 → 验证 → 签名，五步缺一不可。
> ② 任一步未完成 → MUST中止，不能走下一步，不能跳过，不能简化。
> ③ 验证失败 → 从记忆琥珀恢复后重试。
> ④ 全部通过后MUST输出签名，无签名 = 未完成。

> NEVER在未备份的情况下直接修改用户文件——原因是用户数据一旦丢失无法恢复。
> MUST先备份再比对、先比对再操作、先验证再签名。

#### 🔄 旧版迁移（升级前必做·仅旧包用户触发）

> 历史版本曾把 4 个系统文件写入 `[记忆共享中心]`（归档根），属位置错误（系统文件必须位于 `~/.workbuddy/` 才生效）。**本版安装器已修正写目标**，但旧包装出的用户机器上这 4 件可能仍在归档根。

升级合并前，先检测并迁移：

```bash
for f in IDENTITY SOUL USER MEMORY; do
  if [ -f "$MEMORY_CENTER/$f.md" ] && [ ! -f "$HOME/.workbuddy/$f.md" ]; then
    mv "$MEMORY_CENTER/$f.md" "$HOME/.workbuddy/$f.md"   # 归档根 → 系统目录
    echo "↪️ 已迁移 $f.md → ~/.workbuddy/"
  fi
done
```

- 若 `~/.workbuddy/` 已有同名文件 → 不覆盖，按下方「锁一备份锁」走合并逻辑。
- 迁移后这 4 件统一位于 `~/.workbuddy/`，后续读/写/回滚全部以此为准。

**处理范围**：4 个系统文件

| 文件 | 模板来源 | 数据来源 |
|:----|:--------|:--------|
| IDENTITY.md | `references/templates/IDENTITY.md` | 旧文件提取 Emoji/署名格式 |
| SOUL.md | `references/templates/SOUL.md` | 旧文件提取个人品格描述 |
| USER.md | `references/templates/USER.md` | 旧文件提取称呼/身份/关注/需求 |
| MEMORY.md | `references/templates/MEMORY.md` | 旧文件提取自定义记忆条目 |

> CORE.md / SHADOW.md / 摘要版 / 动态状态快照 → 由 step7.5b 升级路径处理

---

#### 🔒 五道锁（升级用户专用）

**每道锁独立验证，全部通过后才能走下一步。缺一不可。**

---

#### 🔒 锁一：备份锁（执行前）

对每个系统文件，逐个执行备份（4次循环）：

```
1. Read 旧文件（位于 `~/.workbuddy/`：`~/.workbuddy/IDENTITY.md` 等）→ 提取用户原有数据（品格描述/称呼/Emoji/自定义记忆等）
2. Write 备份 → {记忆共享中心}/记忆琥珀/升级备份_{时间戳}/{文件名}.bak
3. 确认 .bak 文件存在且非零字节
```

| 文件 | 备份路径 | 验证 |
|:----|:--------|:---:|
| IDENTITY.md | `记忆琥珀/升级备份_{时间戳}/IDENTITY.md.bak` | ✅ 存在 + 非零字节 |
| SOUL.md | `记忆琥珀/升级备份_{时间戳}/SOUL.md.bak` | ✅ 存在 + 非零字节 |
| USER.md | `记忆琥珀/升级备份_{时间戳}/USER.md.bak` | ✅ 存在 + 非零字节 |
| MEMORY.md | `记忆琥珀/升级备份_{时间戳}/MEMORY.md.bak` | ✅ 存在 + 非零字节 |

❌ 任一项备份缺失或零字节 → MUST中止，先补备份再继续
❌ NEVER在未备份的情况下直接修改用户文件——原因是用户数据一旦丢失无法恢复

---

#### 🔒 锁二：比对锁（操作前）

逐章比对模板 vs 用户文件，判定每个文件的操作类型。

**判定规则**：

| 判定 | 条件 | 操作 |
|:----|:----|:----|
| 🟢 **增量更新** | 模板有新增章节，用户文件没有 | 追加模板新增章节到用户文件末尾 |
| 🔴 **结构性更新** | 章节结构调整（新增/删除/重排） | 走完整模板替换（备份→提取→注入→写入） |
| ⚪ **无变化** | 模板 vs 用户文件章节完全一致 | 跳过，不动 |

**逐项判定并输出**：

| 文件 | 模板 vs 用户章节 | 判定 | 操作 |
|:---|:---------------|:----|:----|
| IDENTITY.md | | 🟢/🔴/⚪ | |
| SOUL.md | | 🟢/🔴/⚪ | |
| USER.md | | 🟢/🔴/⚪ | |
| MEMORY.md | | 🟢/🔴/⚪ | |

❌ 比对结果MUST输出在对话框（不能只有AI自己知道）——输出后再继续
❌ NEVER比都不比直接覆盖——节省几步不等于省对几步

---

#### 🟡 锁二点五：差异预览（用户确认）

**在锁二判定完成后、锁三操作执行前，输出差异预览供用户确认。**

对判定为 🟢增量更新 或 🔴结构性更新 的文件，逐文件输出摘要：

| 文件 | 判定 | 差异摘要 |
|:----|:----:|:--------|
| IDENTITY.md | ⚪ 无变化 | — |
| SOUL.md | 🟢 增量更新 | 模板有3个新章节（§D 品格扩展 / §E 思维偏好 / §F 协作红线），用户文件无 |
| USER.md | ⚪ 无变化 | — |
| MEMORY.md | 🔴 结构性更新 | 模板章节重新编排（5章→3章），用户数据将注入新结构 |

> 输出后 **必须询问用户确认**：
> "以上是你的系统文件升级预览。确认无误后，我将执行操作。同意？"
>
> - 用户确认 → 继续锁三
> - 用户有疑问 → 展示具体差异 → 用户决定保留/覆盖/手动合并
> - 用户拒绝 → 中止升级，恢复备份

❌ NEVER在用户未确认前执行操作锁
❌ NEVER跳过差异预览——用户有权知道自己的文件被改成什么样

---

#### 🔒 锁三：操作锁（执行中）

按锁二的判定结果执行：

**🟢 增量更新**：
```
Read 用户文件 → 读模板找出新增章节 → 追加到用户文件末尾 → Write 写入
```
> NEVER覆盖用户已有内容——只追加新增章节。

**🔴 结构性更新**：
```
Read 旧文件 → 提取用户数据 → Read 新模板 → 注入合并 → Write 写入
```
> 以模板骨架为底（保证格式对齐v1.0标准），将提取的旧数据填入对应位置。
> MEMORY.md：保留自定义记忆条目，仅更新硬路牌/路径索引部分。

**⚪ 无变化**：
```
跳过，不做任何操作
```

❌ 操作未执行 → 不能写下一条
❌ 操作执行中出错 → MUST中止，输出错误信息

---

#### 🔒 锁四：验证锁（执行后）

修改后的文件逐项验证。**这是系统性文件升级最重要的步骤——必须逐项检查，不可跳过。**

| 验证项 | IDENTITY | SOUL | USER | MEMORY |
|:------|:--------:|:----:|:----:|:-----:|
| 用户原有数据保留（grep关键字段） | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ |
| 新增章节已追加（grep新增标题） | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ |
| 结构完整（章节数与模板一致） | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ |

❌ 任一项 ❌ → 从记忆琥珀恢复旧文件 → 回到锁一重试 —— 原因是数据安全高于一切
❌ 验证结果MUST输出在对话框（不能只有AI自己知道）——输出后再继续

---

#### 🔒 锁五：签名锁（通过后）

验证全部通过后，MUST在最后一行输出签名：

```
[🧠记忆层 系统文件升级验证通过 | 4/4文件 | 升级备份_{时间戳} | {date命令返回日期}]
```

❌ 无签名 → 视为未完成，不走下一步
❌ NEVER用"已备份""已验证"等简化短语替代签名——简化签名=跳过签名

---

#### 完成后输出模板（强制执行）

> 先输出文本报告，再输出表格。表格行按实际文件数动态生成。

```
🔒 系统文件升级报告
━━━━━━━━━━━━━━
```

📦 **备份**：`记忆琥珀/升级备份_{时间戳}/`

| 文件 | 备份状态 | 验证 |
|:----|:-------:|:---:|
| IDENTITY.md | 已备份 | ✅ 存在 + 非零字节 |
| SOUL.md | 已备份 | ✅ 存在 + 非零字节 |
| USER.md | 已备份 | ✅ 存在 + 非零字节 |
| MEMORY.md | 已备份 | ✅ 存在 + 非零字节 |

🔍 **比对结果**：

| 文件 | 判定 | 操作 |
|:----|:----:|:----|
| IDENTITY.md | ⚪ 无变化 | 跳过 |
| SOUL.md | 🟢 增量更新 | 已追加新增章节 |
| USER.md | ⚪ 无变化 | 跳过 |
| MEMORY.md | 🟢 增量更新 | 已追加新增章节 |

✅ **验证**：

| 验证项 | IDENTITY | SOUL | USER | MEMORY |
|:------|:--------:|:----:|:----:|:-----:|
| 用户数据保留 | ✅ | ✅ | ✅ | ✅ |
| 新增章节完整 | ⚪ 无 | ✅ | ⚪ 无 | ✅ |
| 结构一致 | ✅ | ✅ | ✅ | ✅ |

> 全部通过（4/4）

```
[🧠记忆层 系统文件升级验证通过 | 4/4文件 | 升级备份_{时间戳} | 2026-06-27]
```

> ⚠️ 升级合并不改变用户原有数据内容，只保证格式对齐 v1.0 模板骨架。全部通过后，继续执行 5g-2。

### 5g-2. 通用模板复制（无旧数据需合并）

以下文件无旧用户数据，直接从模板复制或替换变量后写入。

| 目标文件 | 操作 | 说明 |
|:--------|:----|:----|
| `{记忆共享中心}/记忆规则/TTL注册表.md` | Read `references/templates/TTL注册表.md` → 替换 `{记忆共享中心}` 为用户实际路径 → 写入 | 含变量占位符，需替换 |
| `{记忆共享中心}/记忆蓝图/` | **#23统一逻辑**：不存在→`cp -r`；存在→询问是否覆盖→否不动/是则整目录移`记忆琥珀/备份_{时间戳}/`后`cp -r`新版覆盖 | 整目录静态复制，无变量 |
| `{记忆共享中心}/最伟大的我/` | 不存在则 `cp -r references/最伟大的我/ 目标路径`；已存在则跳过（不覆盖用户个人成长数据） | 8子目录骨架，个人数据运行时填充 |
| `{记忆共享中心}/成就系统/` | `cp -r references/成就系统/ 目标路径`（排除 __pycache__） | 子系统整目录复制 |
| `{记忆共享中心}/未知未知/` | 不存在则 `cp -r references/未知未知/ 目标路径`；已存在则跳过（不覆盖用户残差条目） | 悬置区容器+台账，route_unknown_unknown.py 钩子写入目标 |

**输出补充**：
```
├─ 记忆规则/TTL注册表.md → 已创建 ✅
├─ 记忆蓝图/（完整目录） → 已复制 ✅
├─ 成就系统/（scripts + 定义文件） → 已复制 ✅
```

---

### 🔒 门禁

**全新用户**（走5a-5f）：
1. 33题答案是否全部处理？缺任一道 → 回到步骤4补答
2. 所有目标文件（5d清单 + 5d-8清单）是否已存在？缺任一个 → 补写
3. 风格合规检查（5f）是否全部通过？未通过 → 修正后重检

**升级用户**（走5g + 5g-2）：
1. **五道锁全部通过**（见上方5g节）：
   - ✅ 锁一：备份完整（4/4文件确认存在且非零字节）
   - ✅ 锁二：比对完成（逐章判定已输出在对话框）
   - ✅ 锁三：操作执行（增量/结构性更新全部完成）
   - ✅ 锁四：验证通过（用户数据保留 + 新增章节完整 + 结构一致，全部 ✅）
   - ✅ 锁五：签名输出（升级验证签名已输出）
2. 风格合规检查 → 由 step7.5b 盲审处理，本步跳过

⚠️ 五道锁任一项 ❌ → MUST中止，回到5g重试。未完成锁不得走下一步。

> 文件合成完毕 → 写入 `{记忆共享中心}/.install/step5-done.md` → 继续步骤6
